---
interface Props {
  imageUrl: string;
  name: string;
}
const { imageUrl, name } = Astro.props;
---
<div class="card-image relative overflow-hidden">
  <img src={imageUrl} alt={name} class="w-full h-[400px] object-cover" />
</div>

