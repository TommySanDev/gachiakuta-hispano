---
import { Image } from 'astro:assets';

interface Props {
  image: any;
  name: string;
}

const { image, name } = Astro.props;
---
<div class="card-image relative overflow-hidden">
  <Image src={image} alt={name} width={300} height={400} class="w-full h-[400px] object-cover" />
</div>

